## SC2002 Project
This is our implementation of a Turn-Based Combat Arena Game written in Java.

To compile all the packages:

    javac -d out app/*.java actions/*.java actions/exceptions/*.java battleengine/*.java characters/*.java core/*.java items/*.java statuses/*.java ui/*.java

To start the game:

    java -cp out app.GameApp
